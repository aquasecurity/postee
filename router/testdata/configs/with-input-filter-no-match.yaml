Name: tenant

routes:
- name: route1
  outputs: ["my-slack"]
  template: raw
  input: contains(input.image, "image2")
  plugins:
   Policy-Show-All: true

templates:
- name: raw
  body: |
   package postee
   result:=input

outputs:
- name: my-slack
  type: slack
  enable: true
  url: https://hooks.slack.com/services/ABCDF/1234/TTT